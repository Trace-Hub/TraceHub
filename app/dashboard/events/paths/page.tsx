import type { ReactElement } from "react"

const PathsPage = (): ReactElement => {
	return (
		<div className="flex flex-1 items-center justify-center p-6">
			<p className="text-text-secondary">준비 중</p>
		</div>
	)
}

export default PathsPage
